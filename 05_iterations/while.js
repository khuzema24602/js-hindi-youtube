let index=0
while (index<=10) {
    //console.log(`the value of idx is:${index}`);
    index=index+2
}
myarr=["superhero","batman","flash"]
let arr=0
while (arr<myarr.length) {
//    console.log(`the value is ${myarr[arr]}`);
    arr=arr+1
}
let score=1
do {
    console.log(`the score is:${score}`);
    score++
} while (score<=10);